# 路线图 / TODO

## ✅ v0.1 — 搜索 · 下载 · 重命名（已完成）

- [x] ticker → CIK 查询（SEC company_tickers.json，24h 缓存）
- [x] 公司信息卡：名称 / 交易所 / 官网 / 投资者网站 / 最新财报日期
- [x] 10-K / 10-Q / 20-F / 6-K 按季度区间批量下载（支持"到最新"）
- [x] 超出最近 1000 条提交时自动拉取历史分页
- [x] 统一重命名 `代码_表单_报告期.htm` + zip 打包 + manifest.csv 溯源
- [x] SEC 合规：User-Agent 带邮箱、0.12s 请求间隔、单次 60 文件上限
- [x] 深色单页前端（中文，无构建步骤）
- [x] 快捷模式「最新一份 / 最新两份」（默认最新一份，每种类型各取最新 N 份）

## 📦 v0.2 — 文件体验增强

- [x] 6-K 附件自动下载（业绩正文常在 EX-99 里，主文档只是封面；2026-07-17 实装）
- [x] 6-K 智能挑选 v1：同报告期取最大文件 + 精确季度末优先（TSM 类 reportDate=季度末的发行人已可用）
- [ ] 6-K 智能挑选 v2：BABA 类发行人 reportDate=发布日、垃圾公告又大又多，需接 EDGAR 全文搜索
      API（efts.sec.gov）按 "Results Announcement" 等关键词识别业绩 6-K
- [ ] HTML 财报转 PDF（参考站的卖点；可用 headless Chromium / weasyprint）
- [ ] XBRL 原始文件下载选项
- [ ] 包含修正版（10-K/A、10-Q/A）的开关
- [ ] 下载进度条（服务端 SSE 或 websocket 推送逐文件进度）
- [ ] 批量输入多个 ticker，一次打包
- [ ] 公司官网 / 投资者网站为空时的兜底（SEC 字段经常空缺，可从 10-K 封面页解析）

## 📊 v0.3 — 财务数据提取（估值的地基）

- [x] 接 SEC XBRL companyfacts API：`valuation/fetch_facts.py`（2026-07-17 实装）
      —— 多标签合并（公司会换标签）、Q4 自动推导、TTM（损益=四季加总；现金流=年度+YTD差额）
- [x] 核心科目时间序列：营收/营业利润/净利/EPS/CFO/CapEx/现金/债务/股本
- [ ] 数据缓存（SQLite），避免重复请求 SEC
- [ ] 分部数据自动提取（目前由判断层从 10-K/10-Q Segment 附注人工提取）

## 🎯 v0.4.5 — 基本面变化追踪（价值投资主线）

> 工具的核心用途是"最新财报 → 基本面有没有变化 → 更新估值"。目前每次运行是独立快照，
> "变化"这一步要靠人眼对比两份 Excel——以下按价值增量排序补齐闭环。

- [x] 运行间对比 `valuation/compare.py`：基本面(TTM)变化 / 假设漂移 / 目标价变动三张表，
      并对"目标价大动但营收没动"发噪声警报（2026-07-18 实装；bundle zip 里的
      valuation.json 即对比输入，**记得留存每季的 bundle**）
- [x] 摘要页「数据时效」块（报告期末/提交日/距今天数，>120 天标红）+ 方法离散度提示
      （2026-07-18 实装）
- [x] **8-K 盲区（业绩窗口）**（2026-07-23 实装，PENDING_10Q 机制）：业绩 8-K
      （items 含 2.02）比最新定期报告新时——大科技隔 0-2 天、银行/中盘 2-6 周
      （实查 PNC 16-35 天、FHN 21-42、HOMB 19-44、TSCO 14-21）——自动：
      ① 把 EX-99.1 新闻稿打进 filings/SECTIONS（manifest form 标注"未经审计"）；
      ② 判断层 prompt 注入 PENDING_10Q 口径说明并**强制 ttm_revenue_override**
      （公式=旧TTM−去年同季+本季，schema retry 拒绝缺失）；
      ③ 时效块排除 8-K 行（否则 XBRL 停在上季度时报表会显示"1 天前"——反向误导），
      另加红色警告行"业绩已公布,10-Q 未提交"；
      ④ 10-Q 一提交,状态自动消失,零配置回到常规路径。`VALUATION_NO_8K=1` 可关。
      GOOG Q2'26 财报日 5 次独立采样验证：override 5/5 精确一致(445,866)、
      $99B 一次性股权收益 5/5 识别并剔除、0 红旗，base blend CV 4.1%
      （与 v2+opus 无 8-K 基线一致——新路径不增噪）。
      未覆盖（后续）：financials 模式无 override 通道（银行恰是 10-Q 慢的重灾区）；
      非业绩类 8-K（指引下修 Item 2.02 之外/减值 2.06/并购 1.01）仍是盲区；
      adj_ni 的 TTM 营业利润是否同步滚动本季，5 次采样中 2/5 滚动 3/5 未滚
      （~8% 分歧通道），prompt 需把口径钉死
- [x] **假设连续性**：把上一次运行的 config（假设+理由）作为参考注入判断层 prompt（2026-07-19 实装：`VALUATION_PREV_CONFIG` 指向上次 bundle 里的 config_假设留档.json；
      **2026-07-22 v2 起默认开启**：gate-clean config 自动持久化到 `prev_configs/{ticker}.json`，新报告期/现价变动>15%/语义版本不符时自动失效，`VALUATION_NO_CONTINUITY=1` 关闭），
      要求"仅在有新证据时修改假设并说明改动原因"——把运行间噪声（同输入 5 次采样
      base CV≈3.5%）显式转化为可审计的假设变更记录
- [ ] 财报日历感知：显示下一份财报预计日期（上年同期 filing 日推算），临近时提示
      "估值将在 X 天后过时"
- [ ] 周期股常态化盈利：对高波动 OPM 的标的自动补一条"mid-cycle 情景"
      （历史 OPM 中位数 × 当前营收），防止 base 情景锚定周期顶部利润
- [ ] 批量看板：多标的定期重跑 + compare 汇总，"哪些持仓的基本面/估值变了"一页看完

## 💰 v0.4 — 估值分析

- [x] 估值引擎 `valuation/engine.py`：PE 法 + 十年两段式 FCFF DCF + SOTP/倍数法 +
      反向 DCF + WACC×永续g 敏感性；bear/base/bull 三情景（2026-07-17 实装）
- [x] 六表 Excel 报告 `valuation/build_report.py`（摘要/情景假设/DCF/SOTP/历史数据/出处，
      假设全部为可调黄色格、公式联动）+ `verify_report.py` 独立复算验证
- [x] 用户级 Claude Code skill `/valuation TICKER` 串起全流程（LLM 只做判断层：定假设+注出处）
- [x] 已产出样例：META / NVDA / MU（reports/，不入库）
- [x] **历史 PE 分位带** `valuation/pe_band.py`（2026-08-05 实装，其后多轮加固）：
      XBRL 净利÷稀释股数 × yfinance 日收盘 → 逐日 PE → 分位数/σ 带 + `--match` 逆向匹配。
      三种口径不可混用：`ntm`（分母=该日已知最新 TTM 期末之后 12 个月实现的 EPS，
      与 engine 前瞻期严格同源——**生产在用的口径**，锚/交易区间/中枢检查都走它）、
      `forward`（分母=该财年最终实现 EPS，刻意后见之明，对标"财年交易区间"类预测）、
      `trailing`（分母=当日已公告滚动四季 EPS）；forward/trailing 中位数之比≈期间
      EPS 增速（实测 MSFT 1.17x / META 1.21x / AMZN 1.34x）。
      四个静默坑已处理：可知日必须取 filed **最早**（取最新会让旧季度标成两年后才可知，
      MSFT 中位 PE 被算成 43 而非 34）；Q4 股数用 `4×FY−Σ前三季` 补（均值式成立，
      和式不成立）；年度期间需按 `fp=="FY"` 过滤（AMZN 每季申报 twelve-months-ended）；
      分母趋零日按「EPS < 窗口中位 25%」剔除（AMZN 2022 型 300x 假读数）。
      另有：一次性畸变窗口双侧剔除（ANOM_K=1.25，AMZN Anthropic 重估校准）、
      近 3 年子窗分位（避 2021 regime，锚/交易区间用它）、拆股口径**逐条 filed 判定**
      归一（2026-08-14：重述是逐条的，整段跳变探测在 NVDA 10:1 上漏掉未重述旧期，
      band min 5.1→修复后 17.9；反拆股象限旧判据恒真会把重述好的序列再乘 0.1）。
      已接入：`fetch_facts.py` 写入 `facts.pe_band`；`engine.py:pe_band_check`
      输出 `diagnostics.pe_vs_history`（base 中枢=子窗 [P10,P90]∪P50±15%）；
      判断层 base PE 锚子窗 P50（valuation_service 注入）；交易区间行（摘要 27 行起）。
      `VALUATION_NO_PE_BAND=1` 可关。
- [x] `pe_band_check` 接 financials 模式（2026-08-14 实装）：financials 情景开通
      warnings/diagnostics 通道，P/TBV 带检查复用同一套逻辑（label 参数化），
      build_report 金融股摘要增红旗区
- [ ] **观察项：全窗 min/max 那条「从未出现过的倍数」检查是否已过松**（2026-08-10 记，
      先不改，攒观察）。一次性畸变剔除那版把 `ntm` 从 `ttm_eps>0` 的嵌套里解了出来
      （分母是未来窗口的 EPS，与当前已知 TTM 正负无关，负 TTM 期恰是周期底）——
      口径上是对的。AMZN 全窗 max 58x→114x 的回流有两个机制叠加：主因是
      畸变窗剔除拉低了 near-zero 地板的中位基准（floor=0.25×窗口中位 EPS，
      剔除畸变窗后中位下移、地板跟着下移，原被地板挡住的低 EPS 日回流），
      解嵌套是次因——于是 `band["min"] <= pe <= band["max"]` 这条黄旗的上界被
      单点带高，实际上只对荒谬值还有约束力。**锚与交易区间不受影响**（都走
      近 3 年子窗），base 中枢检查也已改成子窗 [P10,P90] ∪ P50±15%；但注意
      全窗分位并非无人消费——prompt 的全窗参照行、Excel 的 full_window_p50
      对照、子窗样本不足时的回退路径都读它，低 EPS 日回流同样移动全窗
      P10-P90，损害有限但不为零。
      要观察的是：bear/bull 是否出现过"离谱但因为全窗 max 被撑大而没打旗"的案例。
      若确认，候选改法是这条也改用子窗 min/max，或对全窗 min/max 先做分位裁剪
      （如 P1/P99）——但那会削弱"从未出现过"这句话的字面含义，需要一并想清楚措辞。
- [ ] 相对估值：PS / EV-EBITDA 历史分位 + 同行对比（PE 分位已完成，见上）
- [ ] 报告 PDF 导出

### ❓ 待定：「价值区间」下沿的语义（决定 pe_band_check 的阈值取向）

> 追踪的那位专业投资者的表里，黄色「26财年价值交易价格」列到底是**区间预测**
> （预测该财年会在此区间内交易）还是**买卖决策边界**（跌破下沿=便宜可买）？
> 两种解读对我们如何设阈值的结论完全相反，目前样本只有 n=1，先记下不下结论。

**倾向决策边界**（本人判断：他给的地板不高，对科技股算合理），支持证据：

- 列名是「**价值**交易价格」，且技术位另有独立一列（「最近筹码强支撑」）——
  两列并存说明黄色列不承担"预测会走到哪"的职能
- MSFT FY26 实测（唯一已完结的样本，2025-07-01~2026-06-30，251 个交易日）：
  跌破下沿 420.42 共 85 天（33.9%），连续分布在 2026-02~06 五个月；
  买在下沿 +17.2%、买在跌破期均价 395.07 +24.7%、买在最低 352.83 +39.7%
  （均截至 2026-08-04 收盘 492.81）。**下沿兑现了它该兑现的东西**
- 上沿 563.53 全年 0 天突破，与"全年未进入贵区、不该减仓"自洽
- 中位 491.98 vs 实际中位收盘 478.43，误差 +2.8%
- 一个从不被触及的下沿是废的；"被跌破 33.9%"在这个解读下是校准良好而非偏高

**若改判为区间预测**，则应按覆盖率校准（目标 80~90%，实测仅 66.1%），下沿需下移，
`pe_band_check` 也该对偏高的下沿报警——**当前实现刻意没有这样做**：只在目标 PE
落到该票历史从未出现过的区间外、或 base 偏离 [P10,P90] 时报 yellow，低倍数本身不报。

**如何证伪**：再攒 2~3 个已完结财年（AAPL FY26 九月底完结、META/AMZN 十二月底），
重点观察他在价格跌破下沿期间是加仓还是改口径。

**另一个待核实的观察**：他 AMZN 那行 PE band（23.5/31.5）与 MSFT 完全相同，
只覆盖 24.9% 的交易日、完整框住 0/4 个财年，而 AMZN 已实现前瞻 PE 中位数是 33.70；
他自己第 2 行用的 25/35/30 反而接近得多——疑似复制粘贴，非方法问题。

### 📐 那张参考表的结构（2026-08-05 与本人确认，避免再读错）

- 每只票 **5 行**：前 4 行是**前四个季度的快照**，带 △ 的末行是**最新数据**。
  不是 5 个并列的估计值——4 行的职能是看估计值的**修正趋势**。
- EPS 来自**付费数据源的 non-GAAP 口径**，与我们 XBRL 的 GAAP/调整后口径差几个点属正常，
  不必强行对齐，当参考即可。
- 定位：**下沿（左侧区间）是这张表最可信的部分**（本人评估），中位次之。
  这进一步支持上面"决策边界"而非"区间预测"的解读。

> 已修正的两处误读，记下防止复发：
> ① 把 5 行当成 5 个并列估计值，据此得出"取最低那行"——错，最低行只是最旧的季度快照；
> ② 早期认为这张表"缺 EPS 修正趋势"——错，那 4 行就是修正趋势本身。

**可迁移到我们系统的点**：他用 4 个季度快照看修正趋势，我们已有同类原料——
`prev_configs/{ticker}.json`（假设留档）+ `compare.py`（两次运行对比）。
差的是 **N 期视图**：

- [x] **N 期趋势视图** `valuation/vintages.py` + `valuation/trend.py`（2026-08-05 实装）：
      一行一个报告期，布局对齐那张参考表，但多做一件它做不到的事——**把季度间的
      变化拿去和同一报告期内的采样噪声比**。
      关键设计：vintage 的键是**报告期**（`meta.vintage.report_end`）而非运行日期，
      同一报告期的多次运行是**样本**不是新 vintage；聚合用中位数（n 小，抗离群），
      组内标准差即判断层噪声的直接估计。相邻期用 SE=sqrt(sd1²/n1+sd2²/n2) 做量纲
      对照：|Δ|/SE <1 噪声内 / 1~2 弱 / ≥2 显著（**不是统计检验**，n=3 时任何秩检验
      都到不了 p<0.05，只是把噪声量级摆到变化旁边）。要有意义，每期至少跑 3 次。
      带 red 红旗的运行照存但打 `gate_clean=false`，读取侧默认排除并报告剔除数——
      直接丢会让样本有偏（坏假设常偏同一侧），全算进去又污染中位数。
      归档在 `vintages/{TICKER}/{report_end}.json`（不能放 jobs/，3 天被 rmtree）；
      服务端在 `_pipeline` 末尾自动写入，历史 bundle 用 `trend.py T --ingest *.json` 回填。
      实测参照：MSFT FY26 的市场估计四季内被上修 +9.1%（17.89→19.51），
      而实际落在 17.95（GAAP）/17.27（剔一次性）——上修路径事后没验证，
      这类"修正方向 vs 最终实现"的对照正是该视图要暴露的东西。
### ⚠️ 带子滞后的剩余暴露面（2026-08-17 事故，见 LESSONS.md）

ntm 口径的带子结构性缺最近约 12 个月。滞后本身**无法消除**（分母要"已实现"
的未来 EPS），已做的是让它在四个消费面全部可见 + 给无滞后 trailing 对照。
剩下这些还没处理：

- [ ] **`ps_band` 同样滞后但未披露**：近零/负利润标的的 P/S 参考价（红旗区那条
      黄旗）取自 `ps_band`，口径同为 ntm，因此也止于约一年前，而该黄旗的文案里
      没有滞后提示。pe_band 那套 span/trailing_nolag 是通用实现，接过去即可。
- [ ] **`pe_band.py` CLI 的 `current` 字段是滞后值**：`b["current"] = series[-1]`
      在 ntm 口径下是约一年前那天（MSFT 实测 2025-10-28 收盘 542.07，而当日实际
      现价 495.40）。engine/report 都不消费它，只有本文件的 CLI 打印用——
      但打印出来长得像"当前"，排查时会误导。
- [ ] **连续性锚（prev_configs）跨越 regime 变化时的行为未定**：连续性纪律优先于
      带子锚，若上一次运行的目标 PE 是在盲区形成前锚定的，它会被原样继承，
      滞后问题因此被延长而不是纠正。需要想清楚"锚过时"要不要成为失锚重建的触发条件。
- [ ] **考虑给"实际/预期交易日"比值一个显式告警**：事故的第 2 条根因是
      `days=1055` vs 5 年应有 ~1260 这道减法从没被做过。与其指望人算，
      不如让带子自己报 `coverage_ratio` 并在明显偏低时留痕。
- [ ] **用一只"营收增速与 EPS 增速明显分叉"的标的验证 trailing→NTM 换算**
      （LESSONS 续篇的最后一条规则）：MSFT 两者恰好接近（15.0% vs 10.7%），
      换算值只差 3.8%，用错量纲肉眼看不出来。挑一只利润率扩张 + 大额回购的票
      （EPS 增速远高于营收增速）跑一遍，确认 `trailing_ntm_eps_growth` 给出的
      折算值与按 g 折算的差距足够刺眼——那才算真正验证过这个修复。

### 🗓️ 前瞻期口径（fiscal period）—— 已修一层，但结构性问题仍在，预计要 refactor

> **这是全系统最容易静默出错的口径。** 记录得详细一点，下次 revisit 直接从这里接。

**引擎的真实语义**：`engine.py` 算 `rev1 = rev0 × (1+g)`，`rev0` 恒为 **TTM 营收**。
所以前瞻期在数学上**永远是 NTM**（TTM 窗口末端起的未来 12 个月），**不是任何一个财年**。
只有 `report_end` 恰好等于财年末时，NTM 才等于一个完整财年。

**已修（2026-08-06）**：`fwd_label` 从判断层输出移到服务器注入
（`valuation_service.fwd_window()`），prompt 显式给出窗口日期区间与它横跨哪些财年，
两个 prompt 的 `g`/`fwd_shares`/`pe` 描述同步改为"对前瞻期"，两个校验器的
`need` 移除 `fwd_label`。失败模式从构造上消失，不再依赖模型稳定性。

**修之前的实际损害**（留作回归判据）：AMZN 同一份财报（report_end 2026-06-30）
跑 3 次 → 1 次 FY2026E、2 次 FY2027E，差整整一年增长，g 裂成 0.06/0.13/0.21，
PE 法 268.4 vs 285.8/304.6，三个目标价不可比。MSFT 因报告期恰是财年末而 4 次全稳，
所以这个 bug 藏了很久——**只在 report_end ≠ 财年末时暴露**。

**仍存在的近似 / refactor 触发条件**：

1. **外国发行人 XBRL 陈旧路径**（`stale_days > 550`）：判断层按 6-K 原文给
   `ttm_revenue_override`，TTM 基准前滚了几个季度**无从得知**，`fwd_window` 此时
   不滚，窗口起点偏早。PENDING_10Q 路径能确定是一个季度（已 `rolled_quarters=1`），
   这条不能。要修得让判断层同时输出 override 对应的期末日。
2. **NTM ≠ 财年，但所有外部锚都是财年口径**：财报 guidance、卖方一致预期、
   参考表全按财年给。现在靠 prompt 要求模型自行换算，没有校验，误差不可观测。
   若发现 g 系统性偏向"直接搬财年数字"，就得考虑方案 B。
3. **方案 B（大改）**：让引擎真的估一个完整财年 = 本财年已实现部分 + 剩余季度预测。
   需要季节性假设、判断层要给分季路径、`rev0` 基准重做。工程量大得多。
   **暂不做的理由**：NTM 前瞻 PE 本就是主流平台标准口径，不是将就。
   触发条件：若要与卖方一致预期做逐项对标，或要复刻参考表的"财年交易区间"语义。
4. **`pe_band.py --basis forward` 与引擎口径不同源**：pe_band 的 forward 用的是
   「该**财年**最终实现 EPS」，引擎是 NTM。report_end = 财年末时重合（MSFT），
   否则错位（AMZN Q2），`pe_vs_history` 的分位数会有偏。已加 `--basis ntm` 对齐。
5. **`fwd_shares` 仍是判断层给的**：它是判断（回购趋势），保留在 LLM 侧合理，
   但其期间定义必须与 NTM 窗口一致——prompt 已改，无校验。

- [x] ~~判断层 prompt 钉死「下一财年」的定义~~（2026-08-06 完成，改法见上：不是改
      措辞，而是把该字段整体移出判断层）
- [ ] 上述近似 1（外国发行人 override 期末日）与 2（财年→NTM 换算无校验）

<details><summary>原始记录（2026-08-05 首次发现时的描述）</summary>

- [ ] **判断层 prompt 钉死「下一财年」的定义**（2026-08-05 AMZN 实测暴露，优先级高）：
      同一份财报（report_end 2026-06-30）跑 3 次，1 次把 fwd 理解成 FY2026E（至2026-12）、
      2 次理解成 FY2027E（至2027-12）——**差整整一年增长**，三个目标价根本不可比
      （PE法 268.4 vs 285.8/304.6），g 假设也跟着裂开（0.06/0.13/0.21）。
      12 月财年公司在 Q2/Q3 报告期最容易分歧：FY2026 已过半算"本年"还是"下一年"没定义。
      修法：prompt 里按 report_end 与财年末的关系给出确定性规则（如"fwd = 距 report_end
      最近的、尚未开始或刚开始的完整财年"），并在 schema 校验里核对 fwd_label 与 report_end
      的推导一致性，不一致直接 retry。trend.py 已能检出并拒绝聚合，但那是事后拦截。

</details>

- [ ] 趋势视图接进 Excel 报告与前端（目前只有 CLI）
- [ ] 该财年实现值回填：财年结束后把实际 EPS 写进 vintage，让趋势表能直接显示
      "当初的修正路径 vs 最终实现"——这是判断修正方向对错的唯一依据

## 🤖 判断层多 LLM 支持

- [ ] 现状：`VALUATION_JUDGMENT_CMD` 钩子已支持任何「stdin 进 prompt、stdout 出 JSON」的命令，
      `VALUATION_MODEL` 可切 claude 的 sonnet/opus/fable；schema 硬校验 + 自动重试对所有模型通用
- [ ] 接入 Codex CLI（`codex exec` 无头模式）、Gemini CLI（`gemini -p`）
- [ ] 接入中国模型：DeepSeek / Qwen / Kimi / GLM 均有 OpenAI 兼容 API，写通用包装脚本
      `judge_openai_compat.py`（base_url / api_key / model 三个参数），一个脚本通吃
- [ ] 正式化：`VALUATION_PROVIDER` 配置（claude-cli | openai-compatible | 自定义命令）+
      前端模型下拉框；不同判断层跑同一公司做质量对比（重点看一次性项目识别能力）

## 🔍 v0.4.1 — 「XBRL 算得出、附注才解释得清」的盲区（2026-09-19 立项）

起因见 LESSONS 的两条 2026-09-19 条目。核心判据：**一个比率能算 ≠ 能用**——
折旧年限、租赁分类、应付时点、分部费用归属都改变了标准比率的分子或分母，
而没有一样有 XBRL 标签。已做 (a)(b)，留 (c)(d) 与本轮新发现的 (e)。

- [x] (a) `extract_sections.KEYWORDS` 加 `useful li` / `accounting estimate` /
      `free cash flow`，且**排在 fact 通道前段**（列表顺序 = 预算优先级）
- [x] (b) `engine.fcf_caliber_warnings`：融资租赁本金未从 `CFO−capex` 扣除时打黄旗
      （META 2025 差 1.26pp，且逐年扩大 0.73→1.26pp；AMZN 反向衰减 7,941→1,557M）
      + `fetch_facts` 加 `finance_lease_principal` 标签
- [x] **(c) 已做，但形态与原设想不同：出数据不出告警。** 四标的实测否决了阈值方案——
      高增长标的的正常利润率爬坡幅度远大于一次性项目（NVDA 2023Q3 +3223bp、
      META 2023Q3 +1743bp 全是真实扩张），任何固定 bp 阈值都是噪声发生器；
      而它仍然抓不到折旧年限变更（+100/+111bp，永久平滑位移）。
      改为 `fetch_facts._opm_series` 把季度 opm + 两种偏离（vs 前四季中位 /
      vs 去年同季）写进 FACTS，prompt 的必做检查 1 从一条判据扩成两条。
      实测能被它抓到而 ni/op 完全看不见的：NVDA 2025Q1 H20 减记（ni/op 0.87、
      opm −1309bp）、META 2026Q2 AI 投入+遣散（ni/op 0.84、opm −1009bp）。
      ~~原文：一次性探测器从 `净利 vs 营业利润` 扩到 opm 阶跃。~~ 现判据是两条损益行的
      比值，**只能看见营业线以下的东西**；营业线之内的（折旧年限变更、诉讼和解费用、
      重组）按构造隐形。AMZN FY2025 营业利润里就含 Q3'25 的 $2.5B 和解费用 +
      Q4 实体店租赁费用（主要打在 NA 分部 → 抬高 `seg1_share`，直接影响 SOTP 的
      m1/m2 权重）。待定：阈值（季度 opm 相对前四季中位偏离多少 bp）、会不会把
      正常的季节性打成噪声。**要先评估对存量标的的黄旗增量。**
- [x] **(d) 已做，且做成了可执行的。** `accounting_estimate_changes` 必填
      （含写 `[]`）+ `accounting_estimate_note` 必填（写 `[]` 时它是"查过"的唯一凭据）；
      `engine.estimate_change_evidence` 拿 SECTIONS 里**带金额**的估计变更摘录与申报
      对账，申报为空而原文有证据 → 黄旗并引用原文。sections.json 作为引擎第五个
      可选参数传入（此前摘录只喂 prompt，引擎看不到原文，所以现有的
      `post_period_capital_events` 必填字段至今只能查形状、写 `[]` 永远能过）。
      yellow 不 red：估计变更不止折旧年限，硬 reject 会让"命中的是无形资产年限"
      这种诚实情形无解（0022 C3 的病理）。
      仅 standard 模式；financials 走 `_validate_judgment_financials` 短路，
      信用损失估计是同型问题，留作后续。
      ~~原文：judgment_prompt 加必答项。~~ 资本密集型标的必须声明锚窗口内有无折旧年限/
      会计估计变更及其披露的 EPS 影响。走 `post_period_capital_events` 那套"必填字段"
      机制——必填才逼得出"没有"这个回答。待定：怎么界定"资本密集型"（capex/营收阈值？
      还是一律必填）、会不会拖长本已很长的 prompt。
- [x] **(e) SECTIONS 预算饱和 —— 已查，结论是预算不是主因。** 四标的
      （GOOG/META/AMZN/NVDA × 各自 10-K+10-Q）实测九套配置，见 LESSONS 2026-09-19（三）。
      一句话：**关键词特异性 >> 预算**。加一条 `change in estimate`（全文只出现 1 次，
      那一次就是量化那句）拿到 2/3 目标数字、成本 +3 字符；抬预算到 200k 拿到 0/3。
      原始的预算饱和问题仍在（下面 (f)），但它影响的是覆盖率不是内容质量。
- [ ] **(f) 预算饱和的余留部分（原 (e)，降级）。**
      给截断加了可见提示（`_budget` 条目）之后立刻暴露：META 两份财报里
      `Subsequent to` / `Subsequent Event` **被截断**——而 KEYWORDS_SUBSEQ 的注释
      白纸黑字写着它是必填字段 `post_period_capital_events` 的唯一来源；整个 risk
      通道尾部（restructuring / going concern / material weakness / covenant）
      两份文件都**一次都没扫过**。
      实测数据（总字符 / 平均命中关键词，23 条里）：
      基线 174k/9.2、`MAX_HITS=2` 166k/12.5、90k 预算 279k/14.8、`MAX_HITS=1` 114k/14.8。
      **`MAX_HITS=1` 覆盖率与 90k 预算持平而只花 41% 成本，但内容质量更差**
      （只取第一次出现＝多半是样板段落，AMZN 反而丢了每股影响数）——
      覆盖率和内容质量是两个方向相反的指标，不能只看前者。
      重配额（方案 C）严格更差，已排除。
      倾向：`MAX_HITS=2` + 继续加特异关键词；抬预算留作最后手段。
      NVDA 两份财报**完全没有** subsequent events 段落（全文 0 命中）——
      必填字段 `post_period_capital_events` 对它本就无源，这种情况要能和
      "被截断"区分开（现在的 `_budget` 条目已经能分）。

## 📐 v0.4.2 — PE 带滞后（2026-09-19 查清，下一个 PR）

四标的实测滞后 305 / 325 / 415 / 423 天，整个 2026 年的 AI 重定价不在任何一个
分布里。拆解见 LESSONS 2026-09-19（五）：**是两个原因，不是一个。**

- 原因 A 后见之明的结构性滞后（约 305 天）：NTM 分母要等 4 个季度落地
- 原因 B 畸变过滤器多吃一个季度（AMZN/GOOGL 的额外 ~100 天）：**不可修也不该修**，
  `t=2025-06-30` 的窗口含 Q2'26 的 Anthropic 重估，补出来照样会被剔

- [x] **(g) 双口径分位 —— 已做（0026）。** `trading_range.trailing_basis` + 全局黄旗 + Excel「双口径分位」行 + stdout 并排。刻意**不提供**任何折算/相减字段，并有一条测试专门钉住这点。 `trailing_nolag` 已经算好了
      但只是警告文案里的一句话。提升为一等公民：报告与 valuation.json 并列出
      NTM 分位与 trailing 分位 + 各自滞后天数，**明确标注不可相减**。
      实测两者分歧很大，值得摆上台面：

      ```
             NTM 带(滞后305-423天)   trailing 无滞后带            滞后
      AMZN   ≈P46                  28.1x vs 29.5/32.6/34.1 <P25  52天
      META   ≈P48                  25.1x vs 22.3/26.2/28.1 ≈P43   2天
      GOOGL  ≈P85                  26.1x vs 26.6/28.1/30.3 <P25  60天
      NVDA   ≈P23                  28.1x vs 32.9/41.0/46.6 <P25   2天
      ```

      两个限定必须写进文案：① 两个分位口径不同，不能相减更不能合并（实测
      trailing/NTM 比值极差 1.47~3.76x，标量折算不成立）；② AMZN/GOOGL 的
      trailing 也被同一笔重估堵住 52/60 天，且要堵到 2027 年年中那笔滚出
      TTM 窗口为止 —— 只有 META/NVDA 是真的 2 天。
- [ ] (h) 一致预期补齐 NTM 分母（优先级低）。yfinance 的 `0q`/`+1q` 够把窗口补到
      `t=2025-12-31`，最后可用日推到约 2026-04-29，滞后 325→约 145 天。
      但工具性质从"纯事后已实现"变成"末段部分前瞻"，要给每个点打 `realized_frac`
      并同时出严格/扩展两套分位。**对问题最大的 AMZN/GOOGL 无效**（原因 B），
      等真的被 META/NVDA 这类干净标的卡住再说。
- [ ] (i) 按调整后 EPS 建带（根治，单独立项）。原因 B 的根源是分母用 GAAP；
      历史每季都按同一口径正常化后，畸变过滤器就没有存在必要，两条带都不会被堵。
      代价是要把 `adj_ni` 做成历史序列，而那是判断不是机械。

      **同一个根因的第二处表现（2026-09-23 五标的实测记）**：`build_report`
      那条「折成 NTM 可比口径约 Xx」点估计用的是 `trailing_ntm_eps_growth`
      （建模 NTM EPS ÷ **GAAP** TTM EPS），实测 META +19%→22.0x、MSFT +7%→25.2x、
      NVDA +25%→32.7x 都正常，而 **AMZN −31%→47.3x、GOOGL −39%→45.9x** —— 因子为负
      不是"EPS 要萎缩 31%"，是 GAAP TTM EPS 被 Anthropic 重估垫高了，除以 0.69/0.61
      把数字顶到远超各自 NTM 带 P90（AMZN P90 才 32.8x）。**负因子的两个标的，恰好就是
      被原因 B 堵住的那两个** —— 这个"可比"数字正被它本该看穿的那笔一次性损益吹起来。
      0026 已给这行加了"点估计、不可折算整段分布、要比位置看双口径分位"的限定并把
      双口径行排在它上面，所以不是新增损害；但限定只是让人别用它，没让它变对。
      分母换成调整后 EPS 后这个因子自动归位，**一并修掉，不要单独打补丁**。

## 🧾 v0.4.3 — IBM 暴露的数据缺口（2026-09-23）

IBM 估值（base $194 vs 现价 $231）复盘时查出三处**数据层**缺口，都不是 IBM 独有：

- [x] **10-K 的 EX-13 没下**：IBM 10-K 主文档把财报/附注/关键会计估计全部"以引用方式
      并入"年报附件，主文档里 settlement/amortization 0 命中。判断层只能估 2025Q4 的
      税收利得（~$2B），还声明"已核对关键会计估计段"。修法：主文档同时出现
      "Annual Report to Stockholders" + "incorporated by reference" 时，按提交索引页的
      **Type 列**取 EX-13 一并打包（文件名无规律：ibm-20251231_d2.htm）。判据不成立零额外请求。
      代价：SECTIONS 的 per_file 预算按文件数均分，IBM 从 2 个文件变 3 个——但主文档
      本就是样板文，预算挪给有附注的 EX-13 是净收益。
- [x] **无 OperatingIncomeLoss 时只推导了 TTM**：IBM 的 op_income 季度/年度整列为空 →
      `_opm_series` 静默为空、prompt 每季印 0 且净利÷营业利润全 n/a、图表营业利润率为空。
      `_derive_op_income_series` 逐期同公式回填（只补最后申报期之后，申报值永不覆盖），
      推导期进 `op_income_derived`，prompt 打 `*` 加图例、图表状态栏注明。
      回填后 IBM 的 prompt 直接暴露两个一次性季度：2024Q3 比值 −0.20（养老金结算）、
      2025Q4 1.31（税务审计结案）。
- [x] **图表总债务双计**：IBM 把整行 "Short-term debt"（含当期到期长债）标成
      ShortTermBorrowings，附注又用 …ObligationsCurrent 标一次 → 2019 起每年多算
      $4.7–7.8B（FY25 面板 $67.7B vs 公司 $61.3B）。发行人级判据
      `_st_borrowings_is_full_line`（DebtCurrent≈短借 且当期到期>0，或 ≥2 期 短借≈当期到期），
      20 只抽查中 INTC/JNJ 也触发但它们有 DebtCurrent 走原分支，数字不变。

**同一轮复盘提出、尚未做的**（按性价比）：
- [x] **营业线口径 PE 带（0031，(i) 的机械版）——参考读数，不是锚**。
      `pe_band` 新指标 `opeps`：分母 = 营业利润×(1−21%)÷稀释股数（营业利润申报优先，
      停报/从未申报的期按 rev−cogs−rnd−sga 推导，与引擎 op1 同源）。21% 是展示常数，
      同一常数用于带子与引擎前瞻分母时在价格里严格约掉（测试钉住）。营业线以内的
      一次性照旧由畸变过滤器剔除。`engine.op_band_reading` 把分位换成每股价格与等价
      GAAP PE（倍数本身不可与 GAAP PE 比）；报告新增一行、prompt 加一行参考。
      滞后：IBM 576→336、AMZN 419→328、GOOGL 427→329，MSFT/META 本在结构下限不变。
      五只票按存档假设 + 2026-09-23 现价：

      | 票 | GAAP 带 位置 / 中枢价 | 营业线带 位置 / 中枢价（≈GAAP PE） |
      |---|---|---|
      | IBM | P68 / $186 | **P11 / $264（27.7x）** |
      | AMZN | P53 / $257 | **<P10 / $282（33.0x）** |
      | GOOG | >P90 / $259 | >P90 / $255 |
      | MSFT | P13 / $582 | <P10 / $579 |
      | META | P60 / $673 | P77 / $679 |

      AMZN 那种分歧不全是滞后：营业线带剔掉了营业外收益的逐年变化（利息收入、较小的
      股权重估），它是另一个镜头而不只是"更新的 GAAP 带"——这也是先做参考不做锚的理由。
      - [ ] 攒 2–3 期存档后评估是否升为锚（或与 GAAP 带并列为双锚）
      - [ ] GAAP 带整体缺席/薄样本时（INTC 型：ntm 有效日仅 21 天）营业线带目前也不出——
            那正是它最有用的场景，值得单独放开
- [x] **SOTP 系统性偏低 —— 诊断已做（0030），修法待定**。vintages 43 个 gate-clean base
      样本 37 个 SOTP<PE。`sotp_addback_diag` 把 TTM 无形资产摊销加回 EBIT 再比
      （只落盘 + 写进 crosscheck 黄旗，不改数）。12 份 prev_configs 的 base 实测：

      | 票 | seg1 | 偏离 | 摊销/op1 | 加回后 |     | 票 | seg1 | 偏离 | 摊销/op1 | 加回后 |
      |---|---|---|---|---|---|---|---|---|---|---|
      | IBM | 0.60 | +29% | 25% | **+3%** | | AMZN | 0.58 | **+36%** | 1% | **+35%** |
      | AVGO | 0.68 | +12% | 10% | +2% | | ORCL | 0.95 | +16% | 5% | +11% |
      | INTC | 0.90 | +24% | 32% | −6% | | META/MSFT/NVDA/APP | ≥0.9 | −7~+14% | 0–3% | 基本不变 |

      结论：并购型（IBM/AVGO/INTC）的偏离几乎全是摊销口径；**AMZN 不是**（摊销仅 1%），
      它的 +36% 在倍数或 seg1_share 本身。SOTP 真正进综合的只有 seg1<0.85 的三只
      （IBM/AVGO/AMZN）。AAPL（最后标该概念 FY2017）、KO（FY2023）被时效闸挡掉，不出诊断。
      - [ ] 修法候选 1：SOTP 腿对并购型改用"摊销前 EBIT"（op1 + 摊销）× m——需判断层声明
            m1/m2 的口径（摊销前/后），否则同样的错配换个方向
      - [ ] 修法候选 2：AMZN 型单独查——从 instance 抽分部营业利润替代判断层给的 seg1_share
- [ ] 自保融资子公司：融资债务只扣不加（IBM ~$13B，CAT/DE/GM/F 更大）——分部名命中
      Financing 类时要求分别申报 financing_debt/financing_assets，两口径并列不自动选。
- [x] **口径冲突表态闸（0029）**：NTM 分位 ≥P60 且 trailing <P25（或 ≤P40 且 >P75）、带子
      滞后 >270 天 → 缺 `pe_regime`（band/recent/blend）打 red 打回一次，已表态降 yellow 并写明
      "相对现价 X% 纯倍数变动"；声明与 base pe 不一致（band 却偏离中枢 >15% / recent·blend
      却贴中枢 ±5%）再加注。引擎算好 现价÷GAAP TTM EPS 与 现价÷调整后 EPS 并排（IBM 20.6x vs
      25.3x）；prompt 删掉"GAAP TTM EPS 见 FACTS"那句（事故直接来源），改为用 adj_ni 口径。
      实跑验证只有 IBM（缺字段 red / 表态后 yellow 两条路径都走过）；其余标的触发与否取决于
      当次 eps1，下次各自重跑时看——按 0026 那张表 GOOGL（NTM ≈P85、trailing <P25）大概率触发，
      NVDA（两边都低）、AMZN（NTM ≈P46）不触发。曾考虑的"盲区 trailing ÷ 主带 trailing"比值
      会把增速放缓误判成重定价（NVDA 0.67、APP 0.69），弃用。
- [ ] 回测定腿权重：DCF/PE 两极分化（AMZN 0.65 / MSFT 0.69 vs IBM 1.42 / ORCL 1.72），
      等权只是平均系统偏差。规则化 config 在 2019–2024 历史 10-Q 上批量跑。

## 🚀 v0.5 — 部署与产品化

- [ ] 部署到 Railway / Fly.io（参考站即 Railway）
- [ ] 简单访问控制（密码或邀请码）
- [ ] 请求日志与失败重试
- [ ] 移动端适配微调
- [ ] （可选）Stripe 付费墙，复刻参考站商业模式
