# LESSONS —— 事故与教训

出了会影响结论的问题就往这里记一条。格式固定四段：**现象 / 机制 / 为什么没早发现 / 改了什么**。
「为什么没早发现」是重点——机制层面的 bug 修一次就没了，检测盲区不写下来会一犯再犯。

---

## 2026-08-17 · 历史 PE 带止于 10 个月前，四个消费面一个都没说

### 现象

MSFT 报告输出「交易区间（近3年PE带×baseEPS）$558~$631.4，中位 $598.8」。
使用者问「这区间是多久的？」——追查发现标称"近3年"的带子，**真实覆盖是
2023-08-17 ~ 2025-10-28，止于 293 天前**，最近约一年的交易日一天都没有。

后果不是标签不好看，是结论被系统性推高：

| | 值 |
|---|---|
| 带子锚 P50（判断层据此定 base 目标 PE） | 30.1x |
| 现价隐含前瞻倍数（价 ÷ base NTM EPS） | **24.9x —— 已跌到带子 P10(25.75) 以下** |
| 盲区那段的 trailing P50 折成 NTM 可比 | **~22x（全窗最低 20.7x）** |
| 交易区间中位相对现价 | +21%，而这 **100% 来自"倍数回到中枢"** |

也就是说：市场最近 10 个月给的倍数已经贴近五年最低，而带子看不见这件事，
照旧报 P50=30.1 当锚，判断层据此给目标 PE 30，交易区间再把它翻译成 +21% 的
上涨空间。**报告没有任何一处提示这个锚可能已经过时。**

### 机制

不是数据丢失，也不是取数失败——是 `basis="ntm"` 的**固有属性**：

```python
for i in range(len(by_end) - 4):
    a, b = by_end[i], by_end[i + 4]
    ntm_after[a["period_end"]] = b["ttm_eps"]   # 分母 = 该日之后 4 个季度的 TTM EPS
```

ntm 口径的分母是「该日之后 12 个月**实际实现**的 EPS」。对最近约一年的交易日，
那个未来还没发生，`ntm_after` 没有条目，这些天于是被
`series = [s for s in series if key in s]` 整段过滤掉。

代码里其实写着这句话（样本不足时的报错文案：「ntm 口径需要该日之后满 4 个季度
已披露，最近一年必然缺」），docstring 也写了 forward 口径「**刻意**用后见之明」。
**设计是清楚的，问题在于这个代价从未出现在任何一个输出里。**

受影响范围（一并核过）：

| 带子 | 口径 | 是否滞后 |
|---|---|---|
| `pe_band` | ntm | **是，约 12 个月**（判断层锚 / pe_band_check / 交易区间） |
| `ps_band` | ntm | **是，约 12 个月**（近零利润标的的 P/S 参考价） |
| `ptbv_band` | trailing | 否（金融股不受影响） |

### 为什么没早发现

这是本条最该记的部分。

1. **覆盖率闸门只管"够不够多"，从不管"够不够新"。**
   `thin_coverage = len(series) < 250` 挡的是薄样本。一条 1055 天、横跨 5 年、
   但**止于 10 个月前**的带子，在这个闸门眼里是完美的。守卫的维度选错了：
   样本量和样本新鲜度是两件事，只测了前者。

2. **数字一直在输出里，只是没人做那道减法。**
   `days=1055` 对 5 年窗口——5×252≈1260，短了 16%。`VERIFY-0814` 里
   「NVDA 693 天」「AMZN 1058 天」「GOOG 490 天」全都带着同样的缺口，
   被当成"覆盖良好"的证据读过好几遍。**一个本该触发疑问的比值，
   因为没有任何地方并排显示"实际/预期"，就一直没被算出来。**

3. **对比方法本身看不见这个洞。**
   排查早期曾用「全窗 P50 29.45 vs 子窗 P50 30.12，只差 2%」得出"regime 混合
   影响很轻"的结论。这个比较**在结构上就不可能发现盲区**——两个窗口都止于
   同一天，差异当然小。要发现它必须换一个**不依赖后见之明**的口径（trailing）
   去量，而这一步在当时没做。

4. **PR review 擦身而过。**
   同一批改动里有一条 review 意见正是关于 `thin_coverage`（薄带子不该放宽
   pe 下限），并因此把这个闸门的四个消费点逐一核对了一遍——**核的是"每个消费点
   都调用了闸门吗"，没有人问"闸门本身够不够"。** 检查清单式的复核只能验证
   既有规则被执行，验证不了规则有没有漏掉一个维度。

### 改了什么

- **数据层** `pe_band`：新增 `span`（真实起止 + `lag_days`，子窗同样给）、
  `drift`（窗口内近段 vs 早段 P50，暴露 regime 移动）、
  `trailing_nolag`（过滤**之前**取全序列算的无滞后 trailing 分位，
  含 `gap_since_main_band` = 盲区那段的单独分位）。
  注意既有的 `other_basis_median` 算在过滤**之后**，同样带滞后，不能当无滞后参照。
- **四个消费面全部披露滞后**：判断层 prompt 注入（`band_meta`）、engine stdout /
  `trading_range` JSON、Excel 摘要、前端一行摘要。其中 prompt 注入是最要紧的一个
  ——它是**输入**面，直接改变判断层给出的目标 PE；第一版修复只补了三个输出面，
  漏了它，复盘时才补上。
- **现价位置与归因**：`fwd_pe`（价 ÷ base 前瞻 EPS，与本带同一分母概念可直接比分位）
  接到带子上给百分位；跌出 P10 / 冲破 P90 打 🟡。交易区间中位相对现价的涨幅
  **按构造恒等于**倍数回归幅度（EPS 在分子分母里消掉），显式写出来。
- **哨兵**：`tests/test_pure.py` 第 10 组是**源码级**断言，检查四个面仍含
  `lag_days`、数据层仍产出三个字段、且 `trailing_nolag` 取自过滤前。
  防的是"重构时把披露顺手删掉"——那种回归不会让任何行为用例变红。

### 可复用的规则

- **任何"历史分布"都必须同时报样本量与窗口真实起止**；只报 `years` / `days`
  等于邀请读者假设它截至今天。
- **任何依赖后见之明的口径（realized / forward / NTM），滞后是它的定义的一部分**，
  必须与数值同框显示，不能只写在 docstring 里。
- **守卫要问"这个维度选对了吗"，不只问"这个守卫被调用了吗"。**
  覆盖率闸门只测了样本量，漏掉新鲜度，整整一年没人发现。
- **验证一个口径有没有盲区，必须换一个不共享同一假设的口径去量。**
  用两个都依赖后见之明的窗口互相比较，看不出任何问题。

---

## 2026-08-17（续）· 修披露的那次改动，自己写错了口径

上一条的修复提交后，PR #5 的评审提了 5 条，**5 条全部成立**，其中 3 条指向同一件事：
**在"把口径讲清楚"这件事上，修复本身又讲错了口径。** 单独记一条，因为这是一个
独立且会重复的失败形态——它不发生在写新功能时，恰恰发生在修口径问题的时候。

### 四个错法

**① 量纲错了，而且我给它配了一条看起来很谨慎的注释。**
trailing PE 换算成 NTM 可比，我除以了 `cfg.scenarios.base.g`——而 `g` 在 prompt 里被
明确定义为**营收**增速（"g 的定义 = 该窗口营收 ÷ TTM营收 − 1"），PE 的分母换算要的是
**EPS 增速**。更要命的是我当时**写了一条注释**说"严格来说该用各日当时实现的增速，
那个数在滞后段同样不存在"——这条注释处理的是**二阶**问题（用哪一天的增速），
却把**一阶**问题（用错了量）掩盖过去了。

> 一条写得像模像样的 caveat 会制造"这里已经想过了"的假象，让读代码的人（包括我自己
> 复查时）跳过对前提的检查。**注释承认了一个次要的不精确，不等于主要的那个是对的。**

**② 同一个错误同时存在于输出面和输入面，而输入面更严重。**
除了显示给人看的换算值，我还在注入判断层的 prompt 里写了"除以 (1+g) 才是 NTM 可比
口径"——那是**指令**，会让 LLM 照着做错的换算。修口径披露时新增的文案，只要落在
prompt 里就不再是文档，而是会改变输出的输入。

**③ 从一个正确的恒等式推出了一个错误的结论，并一次性复制到四个地方。**
`px50/价 − 1 = P50/fwd_pe_now − 1` 是恒等式（EPS 在分子分母里消掉）。我据此写了
"与盈利预测无关"——**错的**：`fwd_pe_now` 本身 = 价÷base EPS，改 base EPS 会让中位价
与它同步变。正确表述是**条件性**的：给定同一个 base EPS 时，两者之差只能是倍数之差。

代数没错，越界的是从代数到自然语言那一步。而我把这句话同时写进了 Excel / stdout /
前端 / README——**一个错误推断一次性传播到四个面**，因为它们是"同源文案"。
同源是好设计（避免各说各的），但它也让错误同步扩散。

**④ 直接显示了一个被截断的值，同一段输出里还有一句与之矛盾的话。**
`_pctile_rank` 对低于 P10 的值一律返回 10（子窗只有 P10~P90 五档）。我把它当精确
百分位显示，于是 `20.0x` 和 `25.7x` 都被报成"第 10 百分位"——而同一个输出块里还打着
"跌出下沿 P10"的黄旗。两句话在同一屏里互相打脸，我没读被调用函数的边界行为。

### 为什么这一轮的自查没拦住

- **哨兵只能守结构，守不住语义。** 我加的源码级哨兵检查"四个面是否仍含 `lag_days`"
  ——它对"披露还在但内容是错的"完全无感。**"报告说了谎"这一整类问题，测试套件
  在结构上就看不见。**
- **我验证的是管道，不是断言。** 字段有没有流下去、`verify_report` 过不过、哨兵绿不绿
  ——全验了。但这 5 条里没有一条是管道坏了，**全部都是"某句话说错了"**。
- **单一测试标的掩盖了量纲错误。** MSFT 的营收增速 15.0% 与 EPS 增速 10.7% 恰好接近，
  换算值只差 3.8%（23.6x vs 24.5x），肉眼完全看不出用错了量。换一只利润率扩张
  叠加回购的票，这个错会大到刺眼。
- **最讽刺的一条：我对一个哨兵做了负向验证，对另一个没做——而没做的那个正好是弱的。**
  `lag_days` 那条我改名试过、确认变红；`trailing_nolag` 那条只断言了
  `_unfiltered = series` 出现在 `trailing_nolag = None` 之前——**把 `_tr` 改回从过滤后的
  序列取，它照样通过**，恰恰漏掉它自称要抓的那个回归。用位置关系代替了行为断言。

### 改了什么

- 换算因子改为 `建模 NTM EPS ÷ **GAAP** TTM EPS`（`trailing_ntm_eps_growth`）。分母取
  GAAP 而非 `adj_eps`——带子的分母是 `NetIncomeLoss`，因子必须与它同源。拿不到正的
  GAAP TTM EPS 时置 `None`，消费侧**不显示**换算值。
- prompt 改为"除以 EPS 增速因子（该情景 NTM EPS ÷ 当前 GAAP TTM EPS），**不是营收
  增速 g**"，并说明为什么会分叉、当前 GAAP TTM EPS 从哪取。
- 恒等式的表述四处统一改为条件性措辞。
- 带外只给关系（`fwd_pe_now_vs_band` / `fwd_pe_now_position`），不给截断分位；
  黄旗不再引用被截断的值。
- `build_trailing_nolag()` 抽成纯函数，弱哨兵换成**行为测试**：合成"最后一个 NTM 观测
  之后还有 30 天 trailing-only 行"的输入断言盲区被抓到，**并反向断言**喂过滤后序列时
  盲区消失——用来证明这条测试确实能判别该回归。

### 可复用的规则

- **caveat 不是免责声明。** 写下"严格来说……"之前先确认宽松版本本身是对的；
  承认二阶不精确会掩盖一阶错误。
- **任何换算因子都要在注释里写出量纲**：分子是什么、分母是什么、与被换算对象
  是否同源。写不出来就说明还没想清楚。
- **从公式推出的自然语言结论要单独审一遍**：把那句话摘出来单独读，前提还成立吗？
  "与盈利预测无关"单独看就是错的，贴在公式旁边却显得顺理成章。
- **同源文案会同步扩散错误**：改一处文案时，要意识到它会同时出现在 N 个面；
  收益是一致性，代价是错误也一致。
- **调用别人的函数前读它的边界行为**（截断、钳位、缺省返回），尤其当你要把返回值
  直接展示给人看的时候。
- **哨兵写完必须先破坏一次被保护的东西，确认它变红。** 没做过负向验证的哨兵
  等同于没有——本轮的实证是：做了负向验证的那条是好的，没做的那条是空的。
- **验管道不等于验断言。** 口径类改动的自查清单里必须有一条：
  *这次新增/修改的每一句面向人的说明，逐句核对它是否为真。*
- **量纲错误需要"两个量明显分叉"的标的才暴露**：只用一只票验证换算逻辑，
  很可能什么都发现不了。

---

## 2026-09-19 · 「重估收益污染了 ETR」是个想当然——税注里根本没这条

### 现象

AMZN 2026-09-19 那次运行，config 给 `tax = 0.19`。我看到这组数就断定它错了：

```
Q2'26  税前 80,857  所得税 18,199  ETR 22.5%     Q2'25  ETR 12.8%
H1'26  税前 120,691 所得税 27,759  ETR 23.0%     H1'25  ETR 17.0%
年度:  2020 11.8% | 2021 12.6% | 2023 19.0% | 2024 13.5% | 2025 19.6%
```

推理是这样的：有大额投资重估的年份（2023/2025）ETR 19-20%，没有的年份
（2020/2021/2024）12-13.5%；重估收益按法定税率计税会把混合 ETR 拉高；所以
19% 是"含重估"的税率，被错用在一个已经把重估剔干净的 EPS 基数上，真实经营
ETR 应该在 13-17%。我据此对用户说"税率这条逻辑不自洽，且杠杆比 other_income
还大、方向相反"。

**读完税注，这个推理的每一步都站不住。**

### 机制

10-Q 税注把金额直接给了：

> Our income tax provision for the six months ended June 30, 2026 was $27.8 billion,
> which included **$15.9 billion of net discrete tax expense primarily attributable
> to the upward adjustments to our investments in Anthropic.**

把这笔和对应的税前收益一起剔掉：

```
H1'26 税前 ex-重估 = 120,691 - 69,062 = 51,629
H1'26 税款 ex-discrete = 27,759 - 15,900 = 11,859
经营口径 ETR = 11,859 / 51,629 = 23.0%
```

**剔干净之后是 23.0%，比 19% 还高。** 15,900 / 69,062 = 23.0% —— 重估收益本身
就是按 23% 计的税，和经营业务同一个率。它不"污染" ETR，它把混合 ETR 往
21%+州税拉，方向和我猜的相反。

10-K 的税率调节表给出了真正的机制——**表里压根没有"投资重估"这一行**，因为
那是普通应税所得。真正让 ETR 上下跳的是抵免项，而它们是**固定金额、不是比例**：

```
                        2023            2024            2025
R&D 抵免         (2,196) -5.8%   (2,644) -3.9%   (2,403) -2.5%
外国收入扣除     (1,429) -3.8%   (2,379) -3.5%     (522) -0.5%
股权激励            784  +2.1%   (2,236) -3.3%   (2,029) -2.1%
合计 ETR                 19.0%           13.5%           19.6%
```

R&D 抵免的**美元金额三年几乎没变**（2,196 / 2,644 / 2,403），但占比从 −5.8%
掉到 −2.5% —— 分母涨了。**AMZN 的 ETR 是随税前利润增长机械上行的**，因为
减项是固定美元、不随规模放大。2024 的 13.5% 之所以低，是"抵免美元数正常 +
税前基数还不大"的组合，不是"那年没有重估"。

按 FY2025 调节项的美元金额自下而上重建前瞻税率：

```
调节项合计 -1,348M
FY2025 经营口径税前 79,975M:  法定 16,795 - 1,348 -> ETR 19.3%
NTM base 税前     115,956M:  法定 24,351 - 1,348 -> ETR 19.8%
```

**config 的 19% 落在自下而上重建值 19.8% 的下方一点点，是站得住的。** 合理区间
是 19-23%（下沿=年度调节表重建，上沿=H1'26 中期实际），19% 在下沿——如果说
有偏差，是**轻微乐观**，不是我说的那个方向。敏感性：19%→21% 使 base 综合从
241.8 到 238.7（−1.3%），→23% 到 235.5（−2.6%）。

### 哪些季度真的被污染

问题问的是"哪些季度不能用"，答案不是按 ETR 高低分，而是按**该季 `other_nonop`
的量级 + 税注有没有单列 discrete**：

```
季度      other_nonop   税注 discrete 披露            经营 ETR 可用性
2024-06        -18      —                             干净
2024-09        -27      —                             干净
2024-12       +468      —                             基本干净
2025-03     +2,749      —                             轻度，无拆分
2025-06     +1,117      H1'25 -753（SBC 超额抵扣）    轻度，方向相反
2025-09    +10,186      —（10-K 未单列该季）          重度，不可拆
2025-12     +1,177      —                             轻度
2026-03    +15,647      H1'26 合计 +15,900            重度，只能按半年拆
2026-06    +53,415      同上                          极重，只能按半年拆
```

关键限制：**discrete 只按半年披露，拆不到季。** 所以 2026 年只有 H1 这一个
可用观测点（23.0%），季度粒度的经营 ETR 拿不到。年度层面最干净的是 FY2024
（other_nonop 全年约 +21M），但那一年的 13.5% 恰恰因为规模效应不可外推。

### 为什么没早发现

- **"ETR 跳动 = 有一次性项目"是个太顺手的模式匹配。** 12.8% 和 22.5% 并排放着，
  又刚好知道 Q2 有 $53B 重估，因果关系看起来不言自明。我把"两件事同时发生"
  当成了"一件事导致另一件"。
- **我先算了比率，才去读注。** 从 XBRL 拉了 `pretax_income_quarterly` 和
  `income_tax_annual` 除一除，图形很干净（有重估的年份高、没有的年份低），
  干净到我没觉得还需要验证。**能用现成数据算出的比率，会抢在读原文之前
  形成结论。**
- **税率调节表能一眼证伪，而我没打开它。** 表里没有"投资收益"这一行 ——
  这一个观察就足以推翻整条推理，成本是翻一页 10-K。
- **我当时对 other_income 的结论是对的（+1,000 偏乐观），旁边这条错的就搭了
  便车。** 同一段分析里有一条经过验证、一条没有，读者没法分辨，我自己当时
  也没分辨。

### 改了什么

本轮只改认知，没动 config：`tax` 维持 19%（自下而上重建支持它），
`other_income` 的 +1,000 偏乐观但影响仅 1.8%，两条都记进 config `notes`
作为下次复核项，不为了"改点什么"而改。

### 可复用的规则

- **"一次性项目污染了比率"这类判断，必须在附注里找到金额，找不到就不能下。**
  发行人单列 discrete 的时候会明说（AMZN 这次给到了 $15.9B）；没单列，就意味着
  要么没有，要么拆不出来——两种情况都不支持"我来估一个"。
- **先读注，再算比率。** 反过来做，比率会先把结论定死；之后读原文就变成
  找证据支持它，而不是检验它。
- **看到 ETR 变动，先翻税率调节表看变的是哪一行。** 没有对应行的解释就是
  编的。这张表是证伪成本最低的一张。
- **固定金额的减项 ≠ 固定比例。** 抵免、扣除、SBC 都是美元金额，公司规模涨
  一倍，它们对 ETR 的贡献减半。**拿小规模年份的历史 ETR 套到大得多的前瞻
  盈利基数上，系统性低估税负**——这对任何高速增长标的都成立，不止 AMZN。
- **中期 ETR 是"估计的年度税率 + 离散项"，不是当季实际。** 10-Q 税注开头
  那段"subject to significant variation"是字面意思，单季 ETR 不能当经营税率读。
- **一段分析里有验证过的结论和没验证的结论并排时，要显式标出来。**
  否则前者会替后者背书。

---

## 2026-09-19（续）· 同一个盲区的一整族：能用 XBRL 算的比率，解释在附注里

### 现象

上一条（ETR）修完，问了一句"还有哪些情况类似"。**当场在同两份年报里找到四条，
其中一条正在影响 META 的估值。**

**① 折旧年限变更 —— 两只票同一个月、反方向，纯附注披露**

| | 变更 | FY2025 披露影响 |
|---|---|---|
| AMZN | 服务器 6 年 → **5 年**（1/1/2025 起） | 折旧 +$1.4B、净利 −$1.0B、−$0.10/股，"**primarily impacted our AWS segment**" |
| META | 大部分服务器 → **5.5 年**（同日起） | 折旧 −$2.92B、净利 **+$2.59B = +$1.00/股** ≈ FY25 EPS 的 4.3% |

而我当时正拿两家的 opm 并排比、又拿 AWS 的 39% 分部利润率去定 `m1`。
**一个被压着、一个被抬着，正好反向。**

**② `hist_fcf_margins` 的口径和发行人自己的不是一个东西 —— 这条是活的**

META 10-K 里有自己的 FCF 调节表：`115,800 − 69,691 − 2,524(融资租赁本金) = 43,585`，
引擎算 `CFO − capex = 46,109`。差 1.26pp，而**这张表是 DCF 终值 margins 的锚，
META 的 DCF 腿是进综合的**。

```
融资租赁本金（$M）
  META   850 → 1,058 → 1,969 → 2,524    逐年扩大
  AMZN 7,941 → 4,384 → 2,043 → 1,557    逐年缩小
```

AMZN 那个方向**更坏**：偏差随时间衰减，会在历史表里凭空造出一条"FCF 率逐年
恶化"的假趋势。恒定偏差至少不改变形状。

**③ AMZN FY2025 营业利润里含一次性** —— Q3'25 $2.5B 诉讼和解 + Q4 实体店租赁费用
（"primarily impacted our North America segment"）。而 `seg1_share = 0.58` 和 SOTP
的分部锚用的正是 FY2025 分部营业利润：**零售分部吸收了这些费用 → AWS 利润占比
被显得偏高 → 直接影响我前一小时刚校准的 m1/m2 权重。** 做校准时没查。

### 机制

这些不是四个独立 bug，是**同一个盲区的一族**。这条 pipeline 的一次性探测器是
`净利 vs 营业利润`——**两条损益行的比值，只能看见会动这个比值的东西**：

- 营业线**以下**的（税、投资重估）→ 看得见 ✓
- 营业线**之内**的（折旧年限、和解费用、重组）→ 比值不动，**按构造隐形**
- **现金流**口径的（租赁分类、应付时点）→ 根本不在这条比值里

PE 带的 `ANOM_K=1.25` 季度离群检测也救不了：年限变更是**永久、平滑的 run-rate
位移**，每季等量，没有哪一季偏离中位。统计离群检测对"所有点一起移动"完全免疫。

### 为什么没早发现

- **"能算"和"能用"在脑子里是同一件事。** 四条序列 XBRL 都有，除法一做比率就出来了，
  没有任何一步会提示"这个分母的定义还有第二种"。
- **上一条 LESSONS 刚写完"先读注再算比率"，我在同一个小时里又犯了 ③。**
  写下规则 ≠ 规则生效。规则要变成**流程里的一个挡板**才算数——这就是为什么
  这轮的产出是关键词和护栏，不是又一条自我告诫。

### 改了什么

- **(a)** `extract_sections.KEYWORDS` 加 `useful li` / `accounting estimate` /
  `free cash flow`。两个细节都是实测撞出来的：
  - 首版写 `"change in estimate"` 得到 **0 命中**，我判成拼写不匹配（"ASC 250 的
    标准标题是 Change in Accounting Estimate"）并换成 `accounting estimate`。
    **这个判断是错的**，见下一条。
  - 首版把三条放在列表**末尾**，META 只有一条挤进来 —— fact 通道按列表顺序消耗
    预算，**排在尾部 = 优先被饿死**。列表顺序就是优先级，这件事此前没写在任何地方。
- **(b)** `engine.fcf_caliber_warnings`：融资租赁本金未从 `CFO−capex` 扣除且差额
  ≥0.5pp 时打黄旗，附近四年差额序列（让"口径漂移 vs 经营变化"可分辨）。
  只对照不覆盖，同 `other_income_crosscheck`。`fetch_facts` 加
  `finance_lease_principal` 标签（`FinanceLeasePrincipalPayments`，
  ASC 842 前的老准则期用 `RepaymentsOfLongTermCapitalLeaseObligations`）。
- **(c)(d)** 记进 TODO v0.4.1，待评估后再动——它们会改变存量标的的黄旗量与 gate 行为。

### 本轮自己踩的两个坑（都是文档里已经记着的）

1. **量纲。** 首版把原始美元直接格式化成 `2,524,000,000M`。
   `other_income_crosscheck` 的 docstring 里一字不差地记着这个坑
   （"不换算会把 AMZN 的差额印成 -81,753,999,000M（首版实测）"）。
   **读过那段注释，还是照着踩了一遍。**
2. **给截断加可见性，立刻暴露出更大的问题。** 见下。

### 附带发现：SECTIONS 预算早已饱和，而且一直是静默的

给预算截断加了 `_budget` 提示条目之后，第一次跑就看到：

- META 两份财报里 `Subsequent to` / `Subsequent Event` **被截断** ——
  而 `KEYWORDS_SUBSEQ` 的注释白纸黑字写着它是**必填字段**
  `post_period_capital_events` 的唯一事实来源；
- 整个 risk 通道尾部（restructuring / going concern / material weakness /
  covenant）两份文件**一次都没扫过**；
- `free cash flow` 同样进不来。

**这不是本轮改动造成的，是本轮改动让它可见了。** 记为 TODO (e)，可能优先于 (c)(d)。

### 可复用的规则

- **判断一个比率能不能用之前，先问它的每一个输入是不是 XBRL 能完整表达的。**
  能算 ≠ 能用。折旧年限、租赁分类、应付时点、分部费用归属——四样都改变了标准
  比率的分子或分母，**没有一样有 XBRL 标签**。
- **探测器只能看见它所在那条线上的东西。** `净利/营业利润` 天然看不见营业线内
  和现金流口径的失真。**给探测器写文档时要写清它看不见什么**，否则"没报警"
  会被读成"没问题"。
- **统计离群检测对"所有点一起移动"免疫。** 会计估计变更、口径变更、准则切换
  都是这种形态，`ANOM_K` 这类逐点阈值永远抓不到——它们只能靠读注。
- **关键词列表的顺序就是预算优先级。** 往有预算上限的列表尾部追加 = 追加了一个
  大概率不生效的东西。加之前先确认预算还有没有余量。
- **"我们用的口径"和"发行人用的口径"要定期对账。** 发行人自己披露同名指标时
  （FCF、调整后 EBITDA、经营利润率），差额本身就是信息。
- **给任何静默截断/降级加可见性，几乎总会立刻暴露出比你在找的更大的问题。**
  本轮实证：为了验证三个新关键词而加的截断提示，翻出了一个必填字段的数据源
  一直在被饿死。

---

## 2026-09-19（三）· 关键词特异性 >> 预算：覆盖率和内容质量是两个方向相反的指标

### 现象

给 SECTIONS 的预算截断加了可见提示后，暴露出预算早已饱和（连必填字段
`post_period_capital_events` 的唯一来源 `Subsequent to` 都被截断）。于是提出三条路
并用 GOOG/META/AMZN/NVDA × 各自 10-K+10-Q 实测九套配置：

```
                     总字符   平均命中关键词(/23)
A 基线 45k/hits3     174k        9.2
C 重配额             171k       10.2    （必达缺口反从 6 涨到 9，排除）
D1 MAX_HITS=2        166k       12.5
D2 MAX_HITS=1        114k       14.8
B3 预算 90k          279k       14.8
```

按覆盖率，**D2 完胜** —— 和 90k 预算同样的覆盖率只花 41% 成本。

然后加第二个指标（摘录里到底有没有判断层要的那个数字），排名整个反过来：

```
              META 事实   AMZN 事实
A 基线          0/3        4/4
D1 hits=2       0/3        4/4
D2 hits=1       0/3        3/4   <- 变差
D2+ctx600       0/3        2/4   <- 更差
B3 90k          0/3        4/4
```

**看起来最好的两个方案实际最差。而 META 的 0/3 所有方案都救不了，包括 90k 预算。**

### 机制

`MAX_HITS` 的语义是**前 N 次出现**，不是最相关的 N 次。META 10-K 里 `useful li`
出现 **31 次**，量化那句排第 **8** —— 前 3 次全是交叉引用，一个数字都没有。
`MAX_HITS=1` 只取第一次，那多半是「Use of Estimates」样板段落。

拆解验证：

```
只抬预算到 200k              -> 目标数字 0/3   （预算根本不是瓶颈）
只按"含金额"给摘录排序        -> 1/3
排序 + 200k + top6           -> 3/3，但 141k 字符
加一条特异关键词              -> 2/3，+3 字符，覆盖率还从 9.2 升到 9.8
```

`change in estimate` 在 META/AMZN 全文**各只出现 1 次，那一次就是量化那句**。
特异性 1 的关键词一跳就到；宽泛关键词砸多少预算都到不了。

### 为什么没早发现

- **只有一个指标时，优化它就是全部。** 覆盖率是个好指标，但它数的是"命中几个
  关键词"，不数"命中里有没有数字"。加第二个指标之前，D2 看起来无可挑剔。
- **我把 `"change in estimate"` 的 0 命中判成拼写不匹配**（"ASC 250 的标准标题是
  Change in Accounting Estimate"），换成了 `accounting estimate`。实际它 0 命中是
  因为排在列表末尾被饿死 —— **原因是预算不是拼写**。这是本次会话第三次同型
  错误：从一个干净的观测直接推因，没验证。前两次是 ETR 和 other_income。

### 改了什么

- `change in estimate` / `useful li` / `accounting estimate` / `free cash flow`
  加进 `KEYWORDS` 且**排在 fact 通道前段**（列表顺序 = 预算优先级，此前没写在
  任何地方）。
- 预算截断不再静默：未扫描的关键词作为一条 `_budget` 条目写回 hits。
- 没动 `MAX_TOTAL`、没动 `MAX_HITS` —— 实测显示它们不是瓶颈。

### 可复用的规则

- **只有一个指标时，它一定会被优化到过拟合。** 抽取类改动至少要两个方向相反
  的指标：覆盖率（广） + 内容命中率（深）。只看前者会挑出最省钱也最没用的方案。
- **关键词列表的顺序就是预算优先级。** 往有上限的列表尾部追加 = 追加了一个大
  概率不生效的东西。
- **加关键词前先数它在全文出现几次 —— 次数越少越值钱。** 31 次的关键词靠
  `MAX_HITS` 抽签，1 次的关键词必中。
- **"抓取用宽关键词、判据用窄关键词"**：同一条关键词可以适合喂给判断层做上下文，
  同时完全不适合驱动机械旗标（见下一条 GOOGL 的假阳性）。
- **给任何静默截断加可见性，几乎总会立刻暴露出比你在找的更大的问题。**

---

## 2026-09-19（四）· 阈值探测器被自己的数据否决；必填字段第一次做成可执行的

### 现象

立项时 (c) 的设想是"一次性探测器从 `净利 vs 营业利润` 扩到 opm 阶跃告警"。
四标的实测对照已知事件：

```
                          ni/op(现有)   opm vs 前四季中位
META 25Q3 OBBBA 税费        0.13          -281bp    现有抓得到
AMZN 26Q2 Anthropic 重估    2.28          +212bp    现有抓得到
NVDA 25Q1 H20 出口管制减记   0.87         -1309bp    只有 opm 抓得到
META 26Q2 AI 投入+遣散       0.84         -1009bp    只有 opm 抓得到
折旧年限变更（两家）          正常       +100/+111bp   两个都抓不到
```

确实补上一整类。但同一批数据里**正常的经营性扩张**是 NVDA 2023Q3 **+3223bp**、
META 2023Q3 +1743bp、AMZN 2024Q1 +394bp —— 高增长标的里趋势幅度远大于一次性
项目，任何固定 bp 阈值都是噪声发生器。而且它**仍然抓不到折旧年限变更**
（+100bp），那恰恰是立 (c) 的起因。

### 机制

一次性探测器只能看见它所在那条线上的东西：

- 营业线**以下**（税、投资重估）→ `净利/营业利润` 比值动 → 看得见
- 营业线**之内**（和解、重组、遣散、减记）→ 比值不动 → 需要 opm
- **永久平滑位移**（折旧年限变更）→ opm 每季等量移动，没有哪一季偏离中位
  → 两条判据都看不见，只能读附注

三层盲区要三种不同的工具，不是一种工具调参数。

### 改了什么

- **(c) 改成只出数据不出告警**：`fetch_facts._opm_series` 给季度 opm + 两种偏离
  （vs 前四季中位 / vs 去年同季），prompt 的必做检查 1 从一条判据扩成两条，
  并明写"没有固定阈值，要结合量级和 SECTIONS 判断"。两个偏离口径要一起看：
  只有中位异常多半是季节性，只有 YoY 异常多半是去年基期有一次性。
- **(d) 必填字段 + 与原文对账**：`accounting_estimate_changes` 必填（含写 `[]`）、
  `accounting_estimate_note` 必填；`engine.estimate_change_evidence` 拿 SECTIONS
  里带金额的摘录与申报对账。**sections.json 作为引擎第五个可选参数传入** ——
  此前摘录只喂 prompt、从不进校验层，所以现有的 `post_period_capital_events`
  这个"必填字段"至今也只能查形状、写 `[]` 永远能过。

### 四标的实跑抓到的假阳性

首版证据判据用了三条关键词。全量跑完按关键词拆开：

```
change in estimate   AMZN 1  META 1  GOOGL 0  NVDA 0   全部是真变更，精确率召回率满分
useful li            AMZN 4  META 0  GOOGL 1  NVDA 0   5 处全是噪声
accounting estimate  全 0
```

GOOGL 那处是**收购无形资产年限表**（"Includes $660 million of acquired cash...
Weighted-Average Useful Life"），AMZN 那 4 处是政策样板碰巧挨着金额。收紧到
两条窄关键词后归零。

**这个假阳性同时验证了"yellow 不 red"这个裁决是对的** —— 做成硬 reject 的话，
GOOGL 这次就会卡死在一张收购无形资产表上，而判断层无法通过"申报变更"来解决
（因为确实没有变更），正是 `_forced_zero_keys`（0022 C3）记着的那个病理。

### 可复用的规则

- **提案的形态要让数据决定，不是让直觉决定。** (c) 立项时是"加个阈值告警"，
  实测后变成"只出数据"——数据否决的是形态不是方向。
- **阈值类判据先看正常值的量级分布。** 高增长标的的正常波动能到几千 bp，
  在那之上找一次性项目，信噪比天然很差。
- **必填字段如果不与原文对账，就只是自觉申报。** 查形状（数组、子字段、note
  非空）拦不住写 `[]`。要可执行，校验侧必须拿得到原文。
- **宽关键词适合喂上下文，不适合当判据。** 同一条 `useful li`：留在抽取列表里
  有用，进旗标判据就是 5/5 假阳性。
- **新判据一定要在多个标的上跑过再合。** 这次的假阳性是第三个标的才出现的。

---

## 2026-09-19（五）· 为了验证带子的滞后，我重写了一遍带子，撞上了它防的那个东西

### 现象

四标的的 PE 带滞后 305 / 325 / 415 / 423 天 —— 整个 2026 年的 AI 重定价不在任何
一个分布里。为了回答"能不能修"，我写了个快速原型：用 yfinance 的季度 EPS 补齐
NTM 分母，看分位会不会变。

原型给出 AMZN 带 P50 = **18.0x**，生产带是 **31.1x**。

### 机制

两个错：

1. **yfinance 只给 6 个季度，生产侧从 XBRL 拿 72 个。** 样本量差一个数量级。
2. **原型没有畸变过滤器。** AMZN 的 NTM 分母 12.43 里直接含着 Q2'26 那笔
   $53.4B Anthropic 重估 —— **而 `ANOM_K` 过滤器存在的全部理由就是剔掉这种窗口**。

我为了测量一个护栏的代价，绕过那个护栏重写了一遍被保护的逻辑，然后撞上了它
防的那个东西。

### 顺带查清的：滞后其实是两个原因

用生产侧数据（不是原型）看：

```
        带尾日        滞后   最后可用季末   近端被剔的窗口
AMZN   2025-07-31   415天   2025-03-31    2026-06-30
GOOGL  2025-07-23   423天   2025-03-31    2026-06-30
META   2025-10-29   325天   2025-06-30    无
NVDA   2025-11-18   305天   2025-07-27    无
```

**差异正好是一个季度，而且是同一个季度。** `t=2025-06-30` 的 NTM 窗口终点是
TTM ending 2026-06-30，含 Q2'26。AMZN 和 GOOGL 都持 Anthropic、都在那季计了
重估、都被过滤器剔掉那个窗口。

- **原因 A：后见之明的结构性滞后**（约 305 天）—— 可用"已实现季 + 一致预期季"
  补到约 145 天，但工具性质从"纯事后"变成"末段部分前瞻"。
- **原因 B：畸变过滤器多吃一个季度**（AMZN/GOOGL 的额外 ~100 天）——
  **不可修，也不该修**；补出来的窗口照样会被剔，而且应该被剔。

另测：把 trailing 用标量折算成 NTM 不成立。取生产带里两者都有值的重叠日，
比值（= EPS 增速因子）极差 1.47x / 3.76x / 1.80x / 1.55x，逐年中位在
0.71~2.16 之间摆动。用今天的因子折算一年前的分布，误差和要测的东西同量级。

### 可复用的规则

- **要测一个护栏的代价，不要绕过它重写被保护的逻辑。** 在生产实现上加开关、
  或者用它自己的输出做对照；平行实现会漏掉所有你不知道自己依赖的东西。
- **快速原型和生产实现差一个数量级时，先假设原型错。** 差 18 vs 31 不是"口径
  略有不同"，是有东西没接上。
- **同一个指标在不同标的上滞后不同，多半有机制，值得查。** 305/325 vs 415/423
  不是随机的，是同一笔重估在两家公司身上的同一个效果。
- **"能不能修"的答案常常是"分两种原因，一种能一种不该"。** 把它当成单一问题
  会得到一个错误的整体结论。

---

## 2026-09-25 · 战略持股：一块价值掉在三条腿的缝里，而加回来的每条路都可能重复计算

### 现象

2026-09-24 逐项对照 AMZN 10-Q 时发现：Anthropic（约 $190B）+ OpenAI（约 $50B）的持股
不进任何一条估值腿，税后约 $18.7/股。每一处单看都"对"：

- PE 腿：`adj_ni` / `other_income` 按纪律剔掉了重估收益（一次性）
- DCF 腿：FCFF 本来就不含非现金重估
- SOTP 腿：EV/EBIT 只看营业利润
- `net_cash`：prompt 定义只收现金 + 有价证券 − 债务

四处剔除各自成立，合起来这块资产就没人管了。

### 机制

「剔除一次性」处理的是**收益流**；收益背后那块资产的**存量价值**，必须在「企业价值 →
股权价值」的桥上另外加回。引擎的桥只有 `net_cash` 一个字段，而它的定义（有意地）
不含非上市股权。于是收益剔得越干净，这块价值丢得越彻底。

### 为什么没早发现

- **护栏都在单条腿内部**：查 other_income 有没有混进一次性、查 net_cash 时点、查 FCF
  口径。没有一条检查问「从盈利里剔掉的东西，它背后的资产在别处加回来了没有」。
- **账面值的大幅上调集中在最近四个季度**（0032 实测 AMZN 私募上调：Q3'25 +7.2B、
  Q1'26 +12.3B、Q2'26 +50.5B），在那之前缺口小得多，混在各腿的误差里看不出来。
- 这块价值只在逐项对照附注原文时才浮出来——又是一次「XBRL 算得出、附注才解释得清」
  （见 2026-09-19（续））。

### 改之前列出的风险

本人的要求是「所有标的只能变好，不能变坏」。把这块价值加回来，每一条路都可能变坏：

| 会变坏的路 | 怎么防 | 剩余风险 |
|---|---|---|
| **与 net_cash 重复**：`lt_securities` 的候选标签里有泛标签 `LongTermInvestments`，有的公司股权投资就记在这一行，判断层可能已经把它算进净现金 | 单独字段，不并进 net_cash；每项必须显式 `in_net_cash=false` 才计入；已在 net_cash 的只留痕、不挪动（所以不会有标的因此被调低） | 判断层把布尔标错 |
| **与 EPS 重复**：权益法损益记在 other_income 里（KO 的装瓶厂、MSFT 的 OpenAI） | PE 腿不加；`income_in_operating_income=true` 的一律不加 | PE 腿因此仍然低估持股，只给 `pe_plus_holdings` 参考数 |
| **与历史倍数重复**：PE 带里的价格本就含市场对持股的定价 | 同上，PE 腿不加 | 同上 |
| **估值偏乐观**：最近一轮估值、流动性、清算优先权、未实现收益的税 | 只认资产负债表账面值；非上市打 20% 折价；按 21% 计税，没给成本就按全额计税 | 账面值本身可能已按最新一轮上调过（AMZN Q2'26）；20% 够不够要看实跑 |
| **判断层报错数**：把「持股比例 × 估值」当账面值、把经营性合资当战略持股、编数 | XBRL 投资类科目合计当上限，超 2% 打红旗打回一次，仍超不计入；没有科目可核对时打黄旗、不计入 | 上限是几个可能互相包含的科目之和，偏松，只挡得住量级错误 |
| **盖住护栏**：持股把经营价值为负的 DCF 抬成正数，n.m. 腿重新参与综合；P/FCF 被抬高 | 护栏一律读 `dcf_ps_operating`；有测试锁住「经营为负、含持股为正」时这条腿仍不进综合 | — |
| **所有标的的采样漂移**：prompt 新增一段，没持股的标的也会读到 | 缺键或 `[]` 时输出逐位不变（引擎 JSON、stdout、Excel 逐格与改动前对拍）；合计不到市值 1% 的可以写 `[]` | 新增必填字段：LLM 漏写会多一次 retry；0033 之前的留档 config 过 `check_configs` 会 BLOCK |
| **历史可比性**：同一标的前后两次口径不同 | `meta.strategic_holdings` 留明细；compare 报每股持股值的变化；trend 把含持股的样本单独分组 | — |

另有三项读数会随持股变，属预期：方法离散度、`blend_p_adjni`、终值敏感性（它们算的是参与综合的腿或综合价本身）。

### 改了什么

0033，细节见 valuation/README 的「战略持股」一节。测试 622 → 674（含真实数据核对后补的 8 条）；15 处刻意改坏的
变异全部被抓到。

**合成数据测试没抓到、真实数据一跑就出来的两个错**（同日放开 data.sec.gov 后）：

- **上限的时效判据拿科目彼此比。** AAPL 的非上市股权最后一次标在 2021-06、AFS 合计停在
  2020-12（$195.6B）；两个都旧，彼此比却都"新鲜"，上限被撑到 $198B——等于没有上限。
  改为以 `data_latest` 为锚。合成 fixture 里所有科目都是同一个日期，这个错不可能出现。
- **报告期后投的钱没有去处。** AMZN 6/30 之后又付了 OpenAI $21.3B。按期后事件纪律它会从
  net_cash 扣掉，而 XBRL 上限只含报告期末——判断层要么把它并进账面值（超上限、整体打回），
  要么不报（这 21.3B 凭空消失）。TODO 原记的「OpenAI $50B」就是含期后的全额承诺。
  新增 `post_period_investment_musd`，只在期后事件已确认扣掉的额度内加回。

**核对通过的**：AMZN 10-Q 的 97.9B（可转债）+ 122.3B（非上市股权）+ 0.4B（权益法）与 XBRL
上限 220.6B 逐项吻合，按口径计入 $14.52/股；引擎 → Excel → verify_report 用真实 facts 全链一致。

**判断层实跑（AMZN/MSFT/AAPL/NVDA/GOOG，完整流水线）**：AMZN 把期后 21.3B 单列并在期后事件里
扣了净现金；MSFT 两项都标 `in_net_cash=true`；AAPL 写 `[]`；PE 腿五只全不变。两次复审红旗都
来自只看经营价值的 DCF 护栏，与持股无关。结果表见 TODO v0.4.4。

**但我对本人说过的一句话不成立**：「已在 net_cash 的只留痕不挪动，所以不会有标的因此被调低」。
NVDA 的判断层把 42.8B 上市股权从 net_cash 挪进了 strategic_holdings——在 net_cash 里 100% 计、
不扣税，挪过来后没给成本、按全额 21% 计税，这一块少 9.0B（约 $0.37/股）。NVDA 整体仍是加值，
但这条承诺只约束了「标 true 的项」，管不住判断层因为读了新段落而改自己的 net_cash 口径。
**改 prompt 的影响面不止新字段本身**：它会改变判断层对相邻字段的决定，只能靠实跑看出来。

### 本轮自己犯的两个错

- 汇报待办时，把一条早已完成的项（「判断层 prompt 钉死下一财年」，08-06 已做）当成了
  未完成。我看到的是 `<details>` 原始记录里那个没勾的复选框。**读 TODO 要看条目的
  最终状态，不能只看第一次出现的复选框。**
- 写 TODO 时顺手写了一个「AMZN 粗估每股约 $15」，这个数没有任何计算支撑，提交前删掉了。
  **没算过的数不进文档**，哪怕标了「粗估」。

### 可复用的规则

- **剔除一次性收益时，要问它背后的资产去哪了。** 把收益流剔掉是对的，但存量价值必须在
  EV→股权价值的桥上另有去处；否则纪律越严，这块价值丢得越干净。
- **把一块价值加回来之前，先把它可能已经待着的地方列全。** 这次是 net_cash、other_income、
  历史倍数三处；每一处要么证明它不在，要么这条腿就不加。
- **「只能变好」要做成可执行的，靠两条：缺省即旧行为，拿不准就不计入。** 前者用新旧实现对拍锁住；
  后者让每一条失败路径都退回改动前的状态，而不是退到某个默认值。
- **护栏读经营口径，展示读含持股口径。** 非经营资产不产生现金流，不能让它替一条失去
  现金流锚的腿投票。
- **加必填字段之前，先数一下它会让多少留档 config 失效。**
- **合成 fixture 的日期全部相同，时效类逻辑在它上面永远是对的。** 有时效判据的函数，
  至少要有一个「科目停标多年」的真实形态用例。
- **「报告期末」口径的核对，要问期后发生的事去哪了。** 期后事件纪律把现金扣掉，
  资产侧就得有对应的去处，否则两套各自正确的纪律会把钱夹没。
